rootProject.name = "RiderRemotePlugin"

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven("https://packages.jetbrains.team/maven/p/ij/intellij-dependencies")
        maven("https://oss.sonatype.org/content/repositories/snapshots/")
    }
}
